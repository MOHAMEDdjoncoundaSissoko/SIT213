#!/bin/bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [ ! -d "$DIR/bin" ] || [ -z "$(ls -A "$DIR/bin" 2>/dev/null)" ]; then
    echo "Le projet n'est pas compilé : lancement de ./compile"
    "$DIR/compile"
fi
java -cp "$DIR/bin" simulateur.Simulateur "$@"
